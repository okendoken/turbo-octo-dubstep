<% if things.any? %>
<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Given</th>
    <th>Description</th>
    <th>By</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% things.each do |thing| %>
      <tr>
        <td><%= thing_link thing %></td>
        <td><%= thing.is_given? %></td>
        <td><%= thing.description %></td>
        <td><%= user_link thing.user %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<% else %>
No things yet
<% end %>